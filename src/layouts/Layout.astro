---
import Blank from "./Blank.astro";
export interface Props {
  title: string;
}

const { title } = Astro.props as Props;
---

<Blank title={title}>
  <style is:global slot="header">
    @tailwind components;

    @layer components {
      .list-horizontal {
        li {
          display: inline-block;

          &:not(:last-child):after {
            content: "·";
          }
        }
      }

      .link {
        @apply underline duration-200 hover:text-accent;
      }
    }
  </style>
  <slot></slot>
</Blank>
